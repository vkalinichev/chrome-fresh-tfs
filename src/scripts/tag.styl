.marked_tag
    display inline-block
    max-width 8em

    padding 0 2px 1px

    background white
    border thin solid hsla( gray, .33 )
    border-radius 3px
    color hsl( 0, 0%, 30% )

    font-size .95em
    text-overflow ellipsis
    vertical-align top
    white-space nowrap

    overflow hidden

    + .marked_tag
        margin-left -2px